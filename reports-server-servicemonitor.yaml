apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: reports-server-servicemonitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: reports-server
  namespaceSelector:
    matchNames:
      - kyverno
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
