apiVersion: v1
kind: Pod
metadata:
  name: test-monitoring-pod
  namespace: load-test-01
  # Missing required labels: app and version
spec:
  containers:
  - name: nginx
    image: nginx:alpine
    ports:
    - containerPort: 80
    resources:
      requests:
        cpu: 10m
        memory: 16Mi
      limits:
        cpu: 50m
        memory: 64Mi
