apiVersion: v1
kind: Pod
metadata:
  name: missing-labels-pod
  namespace: load-test-01
  # Missing required labels: app and version
spec:
  containers:
  - name: nginx
    image: nginx:alpine
    ports:
    - containerPort: 80
    securityContext:
      privileged: false
